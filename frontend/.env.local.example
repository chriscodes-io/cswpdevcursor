# Copy to frontend/.env.local (gitignored) and fill in real values.
# Rotate any tokens that were ever stored in frontend/.env before reusing them.

REACT_APP_MIXPANEL_TOKEN=your_mixpanel_project_token
